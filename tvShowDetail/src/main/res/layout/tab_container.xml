<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:contentDescription="@string/tab_cast_fragment">

    <!-- Height will need to be set programmatically based on the amount of list items you want to show -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/tab_cast_list"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        tools:listitem="@layout/cast_view_holder"/>
</FrameLayout>